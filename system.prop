# Core System Access
ro.secure=0
ro.adb.secure=0
ro.debuggable=1

# USB Configuration
persist.sys.usb.config=mtp,adb
persist.service.adb.enable=1
persist.service.debuggable=1
persist.sys.isUsbOtgEnabled=true
ro.sys.usb.mtp.whql.enable=0
ro.sys.usb.storage.type=mtp
sys.usb.ffs.aio_compat=true

# Display Density
ro.sf.lcd_density=320

# Platform Identifiers (CRITICAL: Removed spaces around '=')
ro.board.platform=mt6765
ro.hardware=mt6765
ro.boot.hardware=mt6765
ro.vendor.mediatek.platform=MT6765
ro.hardware.egl=mali

# Microtrust Security Ecosystem
ro.odm.tee=2
ro.vendor.mtk_microtrust_tee_support=1

# Partition Layout
ro.treble.enabled=true
ro.boot.dynamic_partitions=true

# Force Allow Recovery Property Allocation (Fixes Error 0xb)
ro.twrp.boot=1
